apiVersion: workflow/v1
kind: workflow

metadata:
  name: invalid-executor-unknown-version

projects:
  - name: order-system
    repository: ../examples/order-system

nodes:
  coder:
    node: coding-agent
    executor: v9
