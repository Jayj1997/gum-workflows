apiVersion: workflow/v1
kind: workflow

metadata:
  name: invalid-control-cycle

projects:
  - name: order-system
    repository: ./examples/order-system

nodes:
  approval:
    node: human-approval
    dependsOn:
      - deploy

  deploy:
    node: cd
    dependsOn:
      - approval
