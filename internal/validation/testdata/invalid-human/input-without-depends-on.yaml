apiVersion: workflow/v1
kind: workflow

metadata:
  name: invalid-human-approval-without-depends-on

projects:
  - name: order-system
    repository: ../examples/order-system

# human-approval 必须经非空 dependsOn 挂接被审节点。
nodes:
  input:
    node: human-input

  review:
    node: human-approval
