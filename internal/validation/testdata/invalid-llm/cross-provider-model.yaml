apiVersion: workflow/v1
kind: workflow

metadata:
  name: invalid-llm-cross-provider-model

projects:
  - name: order-system
    repository: ../examples/order-system

nodes:
  coder:
    node: coding-agent
    llm: openai
    target_model: claude-sonnet-5
