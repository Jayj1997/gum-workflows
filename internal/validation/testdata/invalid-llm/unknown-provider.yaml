apiVersion: workflow/v1
kind: workflow

metadata:
  name: invalid-llm-unknown-provider

projects:
  - name: order-system
    repository: ../examples/order-system

nodes:
  coder:
    node: coding-agent
    llm: azure
